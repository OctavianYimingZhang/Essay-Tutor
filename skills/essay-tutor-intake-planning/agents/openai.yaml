interface:
  display_name: "Essay Intake Planning"
  short_description: "Lock briefs and build coursework plans"
  default_prompt: "Use $essay-tutor-intake-planning to reconstruct my assignment brief, ask the plan-changing questions, and build a confirmed structure plan."
policy:
  allow_implicit_invocation: true
